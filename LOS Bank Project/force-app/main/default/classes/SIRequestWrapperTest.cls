@isTest
public class SIRequestWrapperTest {
    /*
     * @isTest
    public static void testSIRequestWrapper() {
        SIRequestWrapper request = new SIRequestWrapper();

        // Set values for the fields
        request.ReferenceNumber = 'REF123';
        request.TransactionBranch = 1234;
        request.RequestId = 'REQ789';
        request.OriginatingChannel = 'Web';
        request.OperationMode = 'Online';
        request.LoanAccountNumber = '1234567890';
        request.RemitterAccountNumber = '0987654321';
        request.ArrearTypeDetails = 'Type A';

        // Perform assertions on the field values
        System.assertEquals('REF123', request.ReferenceNumber);
        System.assertEquals(1234, request.TransactionBranch);
        System.assertEquals('REQ789', request.RequestId);
        System.assertEquals('Web', request.OriginatingChannel);
        System.assertEquals('Online', request.OperationMode);
        System.assertEquals('1234567890', request.LoanAccountNumber);
        System.assertEquals('0987654321', request.RemitterAccountNumber);
        System.assertEquals('Type A', request.ArrearTypeDetails);
    }
*/
}